import { Button } from "@/components/ui/button";

export default function Home() {
  return (
    
    <div className='flex flex-col max-w-xs mx-auto items-center'>
      <Button variant='destructive' className={undefined}>Subscribe</Button>
     Subscribe to new Events
    </div>
  )
}
